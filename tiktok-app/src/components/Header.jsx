import React from 'react';
import './Header.css';

const Header = ({ onMenuClick, currentPage, onPageChange }) => {
  return (
    <header className="header">
      {/* Header complètement vide et minimaliste */}
    </header>
  );
};

export default Header;